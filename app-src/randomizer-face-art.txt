

  // Retired compatibility shim.
  // The authoritative D20 face-number and Forge medallion renderer now lives in
  // three-loader-fix.txt so both older and newer cached app.js loaders use the
  // same implementation. Intentionally no overrides here.
